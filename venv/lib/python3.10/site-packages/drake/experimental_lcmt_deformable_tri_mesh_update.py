"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class experimental_lcmt_deformable_tri_mesh_update(object):
    __slots__ = ["name", "num_vertices", "vertices_W"]

    __typenames__ = ["string", "int32_t", "double"]

    __dimensions__ = [None, None, ["num_vertices", 3]]

    def __init__(self):
        self.name = ""
        self.num_vertices = 0
        self.vertices_W = []

    def encode(self):
        buf = BytesIO()
        buf.write(experimental_lcmt_deformable_tri_mesh_update._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __name_encoded = self.name.encode('utf-8')
        buf.write(struct.pack('>I', len(__name_encoded)+1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">i", self.num_vertices))
        for i0 in range(self.num_vertices):
            buf.write(struct.pack('>3d', *self.vertices_W[i0][:3]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != experimental_lcmt_deformable_tri_mesh_update._get_packed_fingerprint():
            raise ValueError("Decode error")
        return experimental_lcmt_deformable_tri_mesh_update._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = experimental_lcmt_deformable_tri_mesh_update()
        __name_len = struct.unpack('>I', buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode('utf-8', 'replace')
        self.num_vertices = struct.unpack(">i", buf.read(4))[0]
        self.vertices_W = []
        for i0 in range(self.num_vertices):
            self.vertices_W.append(struct.unpack('>3d', buf.read(24)))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if experimental_lcmt_deformable_tri_mesh_update in parents: return 0
        tmphash = (0xd312a58a7220ef24) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if experimental_lcmt_deformable_tri_mesh_update._packed_fingerprint is None:
            experimental_lcmt_deformable_tri_mesh_update._packed_fingerprint = struct.pack(">Q", experimental_lcmt_deformable_tri_mesh_update._get_hash_recursive([]))
        return experimental_lcmt_deformable_tri_mesh_update._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", experimental_lcmt_deformable_tri_mesh_update._get_packed_fingerprint())[0]

